# Keywork Sandbox Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── Required ─────────────────────────────────────────────────────────

# Git identity (used for agent commits inside the container)
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your.email@example.com

# ── Optional ─────────────────────────────────────────────────────────

# Maximum loop iterations (default: 20, set in loop.sh)
# MAX_ITERATIONS=20

# Builds between automatic re-planning (default: 5)
# REPLAN_INTERVAL=5

# Maximum cost in USD before auto-stop (default: 100)
# MAX_COST_USD=100
